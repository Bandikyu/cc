version: "3.7"
services:
  # app:
  #   image: node:18-alpine3.16
  #   command: sh -c "npm run dev"
  #   working_dir: /home/app/8-crud
  #   volumes:
  #     - ../:/home/app
  #   ports:
  #     - 3000:3000
  app:
    # image: alpine:3.16
    # command: sh -c "echo se creo bien"
    # restart: always
    build: .
    working_dir: /home/app/
    volumes:
      - ../:/home/app
    ports:
      - 3000:3000
    tty: true
  #--------------------------------------
  # db:
  #   image: mariadb:10.8.6-jammy
  #   restart: always
  #   environment:
  #     MARIADB_ROOT_PASSWORD: example

  # adminer:
  #   image: phpmyadmin:5.2-fpm-alpine
  #   restart: always
  #   ports:
  #     - 8080:8080
  #--------------------------------------
  # Database
#   db:
#     platform: linux/x86_64
#     image: mysql:5.7
#     volumes:
#       - db_data:/var/lib/mysql
#     restart: always
#     ports:
#       - "3306:3306"
#     environment:
#       MYSQL_ROOT_PASSWORD: password
#       MYSQL_DATABASE: yourdb
#       MYSQL_PASSWORD: password
#     networks:
#       - mysql-phpmyadmin

#   # phpmyadmin
#   phpmyadmin:
#     depends_on:
#       - db
#     image: phpmyadmin
#     restart: always
#     ports:
#       - "8090:80"
#     environment:
#       PMA_HOST: db
#       MYSQL_ROOT_PASSWORD: password
#     networks:
#       - mysql-phpmyadmin

# networks:
#   mysql-phpmyadmin:

# volumes:
#   db_data:
